from setuptools import setup, find_packages

setup(
    name='ivr-local',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Your project dependencies will go here
        # For now, we'll keep them in requirements.txt
    ],
)
